"""agent — LLM agent layer.

Placeholder. The intent is an agent that can read this codebase, propose
strategy ideas, run backtests via the engine, and write up results — but only
through the existing safe interfaces (e.g. it cannot bypass risk overlays or
write to the live execution module).

Empty by design for now; we want the research pipeline solid before adding
an agent on top of it.
"""
